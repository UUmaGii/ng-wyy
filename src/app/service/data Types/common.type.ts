export type Banner = {
    targetId: number;
    url: string;
    imageUrl: string;
}

export type HotTag = {
    position: number;
    name: string;
}

export type songSheet = {
    name: string;
    playCount: number;
    picUrl: string;
    coverImgUrl: string;
}